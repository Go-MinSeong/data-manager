# Data Manager — frontend

React + TypeScript + Vite UI for Data Manager. See the [root README](../README.md) for the full project.

```bash
npm install
npm run dev     # 개발 서버
npm run build   # 프로덕션 빌드 → dist/ (앱 빌드 시 packaging/build.sh가 호출)
```
